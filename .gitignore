# --- AI assistant / agent config (kept local, not shared) ---
# AGENTS.md and CLAUDE.md are tracked on purpose — they are committed, not ignored.
CLAUDE.local.md
.claude/
.cursor/
.cursorrules
.github/copilot-instructions.md
.codex/
.gemini/
GEMINI.md
.aider*
.continue/
.windsurfrules

# --- Secrets & local config ---
# The API token lives in apps/api/config.properties and must never be committed.
config.properties
*.properties
!*.properties.example
.env
.env.*
!.env.example
*.local
secrets.json

# --- Local API response cache ---
.cache/

# --- Node ---
node_modules/
dist/
build/
.cache/
coverage/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# --- Terraform local state and environment values ---
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log

# --- Tooling / framework caches ---
.vite/
.parcel-cache/
.next/
.turbo/
.eslintcache

# --- Editors ---
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*~

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini
