{
  "name": "artificial-analyzer-api",
  "version": "0.1.0",
  "private": true,
  "description": "Thin cached proxy over the Artificial Analysis API",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "snapshot": "node src/collector/local-run.js",
    "test": "node --test"
  }
}
