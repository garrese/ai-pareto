// Public runtime configuration. Localhost deliberately keeps using apps/api.
globalThis.ARTIFICIAL_ANALYZER_CONFIG = Object.freeze({
  dataRoot: 'https://storage.googleapis.com/ia-models-analyzer-public-data',
});
